"""
Banking AI Copilot — FastAPI Backend.

Provides the REST API layer for the Banking Intelligence Platform.
Routes:
  POST /chat              — AI copilot chat (RAG)
  POST /fraud-check       — Real-time fraud scoring
  GET  /customer-summary  — Customer 360 profile
  GET  /trust-score       — Customer trust score
  POST /upload-documents  — Document ingestion
  GET  /health            — Health check
"""
